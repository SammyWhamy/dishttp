import { isAPIInteraction } from './isAPIInteraction.js';
import { isSnowflake } from './isSnowflake.js';

export {
    isAPIInteraction,
    isSnowflake,
}

export default {
    isAPIInteraction,
    isSnowflake,
}
